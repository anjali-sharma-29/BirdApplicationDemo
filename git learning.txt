git init =>create repo on git

any folder which dont need to track , any folder or file which u dont want to add in git put file path in .gitignore file

git status ---to track the files which are changed, unchanged to add to the github
git config --global =>to  set usename and email for local system
git commit -m "message must be descriptive" //it will also add new file if we directly do commit without doing add
git add do only once whenever new file or folder  is added 

git commit -amend  //to modify message 

git remote add origin https://github.com/anjali-sharma-29/BirdApplicationDemo.git
git push -u origin master//-u upstream set
origin=>specify url where to store the files
1)git merge
2)git rebase --merge changes of other user
3)git fetch
4)git cherrypic
5)git stash
6)git squash
7)git clone 
 git clone